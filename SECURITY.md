# Security

Do not commit API keys, model provider tokens, local `.env` files, or prompt
inputs that contain private user data.

Configuration values that contain credentials should be supplied through a
secret manager or local environment variables.
